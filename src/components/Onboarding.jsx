import React from "react";

// This component is a wrapper that handles general onboarding logic
// The role-specific onboarding components are now handled in Layout.js
export default function Onboarding({ userRole }) {
  // Onboarding is now handled in Layout.js with role-specific components
  // This component is kept for backward compatibility
  return null;
}